// iconMap.tsx
import React from 'react'
import { PenTool, FileText, BookOpen, FolderOpen, Clock, User, Settings } from 'lucide-react'

export const iconMap: Record<string, React.FC> = {
  PenTool,
  FileText,
  BookOpen,
  FolderOpen,
  Clock,
  User,
  Settings
}
